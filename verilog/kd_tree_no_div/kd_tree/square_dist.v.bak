module square_dist(clk,pointx,point_y,point_z,counter,accX,accY,accZ);
localparam  dim_size     = $clog2(255),
				dim          = 3,
				max_n        = 10000,
				center_size  = dim*dim_size,
				counter_size = $clog2(max_n),
				acc_size     = $clog2(dim_size*max_n),
















